<!-- context-kit managed · >>> context-kit >>> <<< context-kit <<< -->
This repo uses context-kit. The maps below are already loaded — consult them
before Grep/Read. Run `.context-kit/ck build` if the SessionStart banner flags
them stale.

@AGENTS.md
